# Waves Tracker (Standalone)

- Open `index.html` to run the app.
- Data is saved to your browser (localStorage). Use **Export JSON** to back up.
- If saving doesn't work, disable Private Browsing and ensure cookies/storage are allowed.
